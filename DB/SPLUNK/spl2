index=… "DAG FAILED!" 
| rex field=_raw "
    \[[^\]]+\]\s+###\s+Environment:\s*(?<Environment>\S+)\s+
    Message:\s*(?<Message>.*?)\s+
    Dag_id:\s*(?<Dag_id>\S+)\s+
    Run_id:\s*(?<Run_id>\S+)\s+
    Task_id:\s*(?<Task_id>\S+)
"
| table Environment, Message, Dag_id, Run_id, Task_id
