test

SELECT 
    'UPDATE your_table_name SET ' ||
    LISTAGG(column_name || ' = ''X''', ', ') 
        WITHIN GROUP (ORDER BY column_name) || ';' AS update_stmt
FROM user_tab_columns
WHERE table_name = 'YOUR_TABLE_NAME'
  AND column_name LIKE '%CCY%';
